# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
**/__pycache__/
*.pyc
*.pyo
*.pyd
*.pyw
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
*.egg
pip-log.txt

# Virtual environments
.venv/
venv/
env/
**/env/

# ── Node.js ───────────────────────────────────────────────────────────────────
node_modules/
npm-debug.log*
yarn-error.log*

# Build outputs
dist/
frontend/dist/
frontend/.vite/
coverage/
*.tsbuildinfo
.next/
out/

# ── Environment files ─────────────────────────────────────────────────────────
# Never commit actual secrets — use .env.example as the template
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
**/.env
**/.env.local

# ── Test artifacts & uploads ──────────────────────────────────────────────────
uploads/
backend/uploads/
backend/test_sample.pdf
research-papers/
paper_outputs/

# Never commit PDFs to the repo
*.pdf

# ── OS & editor noise ─────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/
*.swp
*.swo
*~

# ── Project-specific ──────────────────────────────────────────────────────────
# Local virtual environment inside service folder
services/extraction-pipeline/env/

# Git backup directory (should not be committed)
.git_backup/

# One-off debug / scratch scripts
scratch_test.py
**/scratch_test.py

# Verifier debug log (generated at runtime if VERIFIER_LOG_PATH is set)
verifier_log.jsonl

# Output JSON from one-off test runs
/out.json

# Internal agent config
/.agents/

# Jar files (GROBID etc.)
*.jar
